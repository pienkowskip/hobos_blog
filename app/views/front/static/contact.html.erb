<% content_for(:site_title_prefix) { t('front.nav.contact') } %>
<% content_for(:site_header) { caption('headers.contact').html_safe } %>

<div class="content-outer">
  <div id="page-content" class="row page">
    <%= caption('static_sites.contact').html_safe %>
  </div>
</div>